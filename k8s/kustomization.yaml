apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - pvc.yaml

commonLabels:
  app.kubernetes.io/name: oauth-token-relay
  app.kubernetes.io/part-of: oauth-token-relay
